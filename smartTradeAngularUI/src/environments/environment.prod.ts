// Production environment (Cloudflare tunnel)
export const environment = {
  production: true,
  // API served at https://smart-trade.ustsea.com/api
  apiUrl: '/api',  // Relative path - works with same-origin
  apiBaseUrl: '/api'
};
